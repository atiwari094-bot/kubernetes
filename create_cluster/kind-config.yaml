#Create a kind cluster with one control plane and three worker node
#"kind create cluster --name my-cluster --config kind-config.yaml"
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
- role: worker  
